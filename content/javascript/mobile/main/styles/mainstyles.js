// general: styles for home.js.

// node packages
import { StyleSheet, Platform, Dimensions } from 'react-native'

// Dimensions global style values
const widthDimensions = Dimensions.get('window').width

export default StyleSheet.create({
  // ---------- home.js styles
})
